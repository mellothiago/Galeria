# Calculadora Simples

Uma calculadora de terminal feita em Python com as operações básicas:

- Soma (`+`)
- Subtração (`-`)
- Multiplicação (`*`)
- Divisão (`/`)

## Como executar

```bash
python3 calculadora.py
```

Digite `sair` para encerrar.
